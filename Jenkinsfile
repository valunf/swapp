pipeline {
    agent any
    stages{
        stage('Build container') {
            steps {
                //TODO Add building container and running tests
                echo env.GIT_BRANCH
            }
        }
    }
}